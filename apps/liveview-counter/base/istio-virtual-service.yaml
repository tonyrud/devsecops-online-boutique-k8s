apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: frontend
spec:
  hosts:
  - dev.tonyrudny.com
  gateways:
    - istio-ingress/istio-gateway
  http:
  - route:
    - destination:
        host: counter
        port:
          number: 80
    match:
        - uri:
            prefix: /