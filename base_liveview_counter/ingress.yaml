apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: counter-ingress
spec:
  rules:
  - host: replaceme.with.patch.overlay
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: counter
            port:
              number: 80